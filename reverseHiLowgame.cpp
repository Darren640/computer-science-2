#include <iostream>
#include <stdlib.h>   // Used to generate random number using rand method
#include <string> // Used to be able to use string
#include <time.h>  // Used to reset the machine time to generate different random numbers
#include <random>  // Gives access to even better random number generators, if you want

using namespace std;



int main()
{
    srand(static_cast<int>(time(NULL)));
    cout << "Enter a postive number: " << endl;

    int n;
    cin >> n;

    cout << "Write down a number between 1 and n with penicl on sheet of paper " << endl;

    while(true)
    {
        if(n > )
    }
}
